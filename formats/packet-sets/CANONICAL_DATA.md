# Canonical Packet Set Data Location

The website consumes packet set data from:
- `apps/website/frontend/src/features/packets/data/packet_sets.json`

The generator script currently writes to that path:
- `scripts/generate_packets_from_s3.py`

